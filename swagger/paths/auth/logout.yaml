tags:
  - Auth
summary: Logout user
operationId: logoutUser
description: Logout user by clearing session
security:
  - cookieAuth: []
responses:
  '204':
    description: Successfully logged out. All authentication cookies have been cleared.
    headers:
      Set-Cookie:
        description: >
          Cookies cleared by the server.
          Example:
          Set-Cookie: accessToken=; HttpOnly;
          Set-Cookie: refreshToken=; HttpOnly;
          Set-Cookie: sessionId=; HttpOnly
        schema:
          type: string
          example: sessionId=; HttpOnly; 
  '401':
    $ref: ../../components/responses/401.yaml
  '500':
    $ref: ../../components/responses/500.yaml
